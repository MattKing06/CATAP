# This is a template for auto-generating the __init__ files
# for individual modules of CATAP.

from ${LIBNAME} import *
try:
	from ${EXTENSION_NAME} import *
except ImportError:
	print("No extension module for ${LIBNAME}")